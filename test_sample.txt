CNN在目标检测框架中扮演着“特征提取器”的角色，常常作为网络的骨架。当前，无论采用何种具体的检测算法，其首要步骤都是将输入的原始图像送入一个CNN网络中（如VGG-16[48]，ResNet50[49]），以获取高维的、富含语义信息的特征表示。这个过程产生的特征图不仅是后续定位和分类任务的直接输入，其质量也直接决定了整个检测系统的上限。因此，选择一个强大且高效的主干网络是构建高性能目标检测器的前提。
